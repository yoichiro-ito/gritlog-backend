spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gritlog?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      data-locations: classpath:db/data.sql
